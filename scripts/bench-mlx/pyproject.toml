[project]
name = "local-llm-bench-mlx"
version = "0.1.0"
description = "Benchmark scripts for Apple Silicon / MLX runtime (docs/06-evaluation.md)"
requires-python = ">=3.11"
dependencies = [
    "mlx>=0.18",
    "mlx-lm>=0.20",
    "huggingface-hub>=0.24",
]

[tool.uv]
package = false
