[project]
name = "local-llm-bench"
version = "0.1.0"
description = "Benchmark scripts for docs/06-evaluation.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
package = false
